import * as THREE from 'three';
import { OrbitControls } from 'three/examples/jsm/controls/OrbitControls.js';

const scene=new THREE.Scene(); scene.background=new THREE.Color(0xe9d5bd); scene.fog=new THREE.Fog(0xe9d5bd,8,16);
const camera=new THREE.PerspectiveCamera(38,innerWidth/innerHeight,.1,50); camera.position.set(6.5,5.1,8.2);
const renderer=new THREE.WebGLRenderer({antialias:true}); renderer.setPixelRatio(Math.min(devicePixelRatio,2)); renderer.setSize(innerWidth,innerHeight); renderer.shadowMap.enabled=true; renderer.shadowMap.type=THREE.PCFSoftShadowMap; renderer.outputColorSpace=THREE.SRGBColorSpace; renderer.toneMapping=THREE.ACESFilmicToneMapping; renderer.toneMappingExposure=1.05; document.querySelector('#scene').appendChild(renderer.domElement);
const controls=new OrbitControls(camera,renderer.domElement); controls.enableDamping=true; controls.target.set(0,1.1,0); controls.minDistance=5.8; controls.maxDistance=12; controls.maxPolarAngle=1.45; controls.minPolarAngle=.25;
scene.add(new THREE.HemisphereLight(0xffead7,0x604a3a,2)); const sun=new THREE.DirectionalLight(0xffdfbd,3); sun.position.set(-4,7,5); sun.castShadow=true; sun.shadow.mapSize.set(2048,2048); scene.add(sun); const lamp=new THREE.PointLight(0xffb66e,16,9,2); lamp.position.set(-.6,2.8,-1.8); lamp.castShadow=true; scene.add(lamp);
const mat=(c,r=.9)=>new THREE.MeshStandardMaterial({color:c,roughness:r});
const box=(w,h,d,c)=>{const m=new THREE.Mesh(new THREE.BoxGeometry(w,h,d),mat(c)); m.castShadow=m.receiveShadow=true; return m};
const rounded=(r,c)=>{const m=new THREE.Mesh(new THREE.SphereGeometry(r,20,14),mat(c)); m.castShadow=m.receiveShadow=true; return m};
// room
const floor=box(7,.25,4.6,0x9b694c); floor.position.y=-.13; scene.add(floor);
for(let i=-6;i<=6;i++){const plank=box(.045,.012,4.45,0x79503d); plank.position.set(i*.5,-.005,0); scene.add(plank)}
const back=box(7,3.8,.22,0xd8b28e); back.position.set(0,1.8,-2.18); scene.add(back); const left=box(.22,3.8,4.4,0xd1a780); left.position.set(-3.5,1.8,0); scene.add(left); const right=box(.22,3.8,4.4,0xd1a780); right.position.set(3.5,1.8,0); scene.add(right);
// round door
const door=new THREE.Mesh(new THREE.CircleGeometry(.72,32),mat(0x8b5b43)); door.position.set(-2.55,1.2,-2.32); scene.add(door); const knob=rounded(.06,0xe5bd66); knob.position.set(-2.15,1.18,-2.38); scene.add(knob);
// furniture
const items=[];
function item(id,name,x,z,build,actions){const g=new THREE.Group();g.position.set(x,0,z);g.userData.id=id;g.add(build());scene.add(g);g.traverse(o=>{if(o instanceof THREE.Mesh)o.userData.id=id});items.push({id,name,group:g,actions});}
item('kitchen','キッチン',-1.55,-1.35,()=>{const g=new THREE.Group();const c=box(1.45,.7,.62,0x875a43);c.position.y=.35;g.add(c);const top=box(1.55,.12,.7,0xb77d56);top.position.y=.73;g.add(top);const pot=rounded(.19,0x8b6655);pot.scale.set(1,.65,1);pot.position.set(-.35,.91,0);g.add(pot);const handle=box(.35,.04,.04,0x694838);handle.position.set(.2,1.02,0);handle.rotation.z=-.45;g.add(handle);return g},['cook','eat']);
item('cheese','チーズ皿',-.45,-1.25,()=>{const g=new THREE.Group();const plate=new THREE.Mesh(new THREE.CylinderGeometry(.34,.34,.07,32),mat(0xf0dfc6));plate.position.y=.83;g.add(plate);const cheese=new THREE.Mesh(new THREE.CylinderGeometry(.2,.2,.12,24),mat(0xf2c957));cheese.position.y=.92;g.add(cheese);for(const p of [[-.07,.98,-.1],[.08,.98,-.07],[.03,.99,.1]]){const h=rounded(.025,0xc89435);h.position.set(p[0],p[1],p[2]);g.add(h)}return g},['eat']);
item('books','本棚',2.35,-1.72,()=>{const g=new THREE.Group();const shelf=box(1.25,1.6,.3,0x79513e);shelf.position.y=.82;g.add(shelf);for(let i=0;i<4;i++){const b=box(.22,.65,.34,[0xa77989,0x718e7f,0xc48b68,0x8c729d][i]);b.position.set(-.35+i*.22,.5+i%2*.05,-.19);g.add(b)}return g},['read']);
item('bed','マッチ箱ベッド',1.25,-1.1,()=>{const g=new THREE.Group();const base=box(1.25,.38,.72,0x9c6749);base.position.y=.28;g.add(base);const sheet=rounded(.48,0xd8c1aa);sheet.scale.set(1.35,.25,.78);sheet.position.set(.08,.52,0);g.add(sheet);const pillow=rounded(.2,0xf0e2ce);pillow.scale.set(1.4,.55,1);pillow.position.set(-.36,.63,0);g.add(pillow);return g},['nap','read']);
item('broom','おそうじ',1.95,.25,()=>{const g=new THREE.Group();const s=new THREE.Mesh(new THREE.CylinderGeometry(.025,.025,.9,10),mat(0x80553d));s.position.y=.5;s.rotation.z=-.25;g.add(s);const b=new THREE.Mesh(new THREE.ConeGeometry(.13,.25,12),mat(0xcaa56e));b.position.set(-.02,.06,0);g.add(b);return g},['clean']);
// mouse
const mouse=new THREE.Group(); scene.add(mouse); mouse.position.set(.2,0,.25); const body=rounded(.32,0xc79a72); body.scale.set(1,.95,.82); body.position.y=.55; mouse.add(body); const overall=box(.48,.3,.28,0x6f8e85); overall.position.set(0,.52,.02); mouse.add(overall); const head=new THREE.Group();head.position.y=1.0;mouse.add(head);const face=rounded(.37,0xd1a176);face.scale.set(1.08,.98,.92);head.add(face);
for(const s of [-1,1]){const e=rounded(.19,0xc58b6c);e.scale.set(1,.95,.55);e.position.set(s*.3,.12,-.01);head.add(e);const inn=rounded(.11,0xefb0a0);inn.scale.set(1,1,.4);inn.position.set(s*.3,.12,-.105);head.add(inn)}
const snout=rounded(.15,0xc58d6d);snout.scale.set(1,.75,.8);snout.position.set(.19,-.05,-.18);head.add(snout);const nose=rounded(.038,0x553832);nose.position.set(.32,-.05,-.3);head.add(nose);for(const s of [-.13,.13]){const eye=rounded(.038,0x382725);eye.position.set(s,.09,-.34);head.add(eye)}
const arms=[new THREE.Group(),new THREE.Group()],legs=[new THREE.Group(),new THREE.Group()];arms.forEach((a,i)=>{const m=rounded(.085,0xc3926f);m.scale.set(.8,1.5,.8);a.add(m);a.position.set(i? .24:-.24,.54,0);mouse.add(a)});legs.forEach((l,i)=>{const m=rounded(.085,0xb78362);m.scale.set(.9,1.4,.9);l.add(m);l.position.set(i?.14:-.14,.25,0);mouse.add(l)});const tail=new THREE.Mesh(new THREE.TubeGeometry(new THREE.CatmullRomCurve3([new THREE.Vector3(0,0,0),new THREE.Vector3(.45,.1,.08),new THREE.Vector3(.72,.22,0)]),12,.035,8),mat(0xb87957));tail.position.set(-.3,.3,.1);mouse.add(tail);
let paused=false, current='wander', target=new THREE.Vector3(), remaining=0, phase=0; const labels={cook:['🍳 お料理する','台所でことこと'],eat:['🧀 チーズを食べる','もぐもぐ、おいしい！'],read:['📖 読書する','静かな時間です'],nap:['💤 お昼寝する','すやすや…'],clean:['🧹 お掃除する','きれいにしよう！'],wander:['🐾 お散歩する','気ままに歩いています']};
const pos={kitchen:new THREE.Vector3(-1.4,0,-.9),cheese:new THREE.Vector3(-.45,0,-.85),books:new THREE.Vector3(1.9,0,-1.05),bed:new THREE.Vector3(1.0,0,-.7),broom:new THREE.Vector3(1.7,0,.1)};
function action(id,itemId){current=id;target=(pos[itemId||'']||new THREE.Vector3((Math.random()-.5)*3,.0,(Math.random()-.5)*1.6)).clone();remaining=labels[id]?({cook:6,eat:5,read:8,nap:10,clean:7,wander:4}[id]||4):4;phase=0; document.querySelector('#status-main').textContent=labels[id][0];document.querySelector('#status-sub').textContent=labels[id][1]}
function auto(){const ids=['cook','eat','read','nap','clean','wander'];const id=ids[Math.floor(Math.random()*ids.length)];const map={cook:'kitchen',eat:'cheese',read:'books',nap:'bed',clean:'broom'};action(id,map[id])}
const ray=new THREE.Raycaster(),pointer=new THREE.Vector2();let menu=document.querySelector('#action-menu');let selected=null;function openMenu(it,x,y){selected=it;document.querySelector('#menu-title').textContent=`「${it.name}」で何する？`;const b=document.querySelector('#action-buttons');b.innerHTML='';it.actions.forEach((id)=>{const q=document.createElement('button');q.className='action-btn';q.textContent=labels[id][0];q.onclick=()=>{action(id,it.id);menu.classList.add('hidden')};b.appendChild(q)});menu.classList.remove('hidden');menu.style.left=Math.min(x,innerWidth-220)+'px';menu.style.top=Math.min(y,innerHeight-180)+'px'}
renderer.domElement.addEventListener('pointerdown',e=>{if(e.button!==0)return;pointer.x=e.clientX/innerWidth*2-1;pointer.y=-(e.clientY/innerHeight)*2+1;ray.setFromCamera(pointer,camera);const hit=ray.intersectObjects(items.map(x=>x.group),true)[0];if(hit){let o=hit.object;while(o&&!o.userData.id)o=o.parent;const it=items.find(x=>x.id===o?.userData.id);if(it)openMenu(it,e.clientX,e.clientY)}else menu.classList.add('hidden')});
document.querySelector('#pause').addEventListener('click',()=>{paused=!paused;document.querySelector('#pause').textContent=paused?'▶ 再生':'⏸ 一時停止';document.querySelector('#status-sub').textContent=paused?'時間が止まっています':labels[current][1]});
auto();let last=performance.now();function loop(t){requestAnimationFrame(loop);const dt=Math.min((t-last)/1000,.05);last=t;if(!paused){remaining-=dt;const d=target.clone().sub(mouse.position);d.y=0;if(d.length()>.08){mouse.position.add(d.normalize().multiplyScalar(dt*.58));mouse.rotation.y=d.x>=0?0:Math.PI}else{phase+=dt;const sway=Math.sin(phase*8)*.12;arms[0].rotation.z=sway;arms[1].rotation.z=-sway;head.rotation.z=Math.sin(phase*2)*.04;tail.rotation.y=Math.sin(phase*4)*.15;if(current==='nap')mouse.rotation.z=Math.sin(phase)*.03;if(remaining<=0)auto()}}controls.update();renderer.render(scene,camera)}requestAnimationFrame(loop);addEventListener('resize',()=>{camera.aspect=innerWidth/innerHeight;camera.updateProjectionMatrix();renderer.setSize(innerWidth,innerHeight)})
